<div class="container">
  <div class="row">
    <%= form_for(microspot) do |f| %>
  <% if microspot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(microspot.errors.count, "error") %> prohibited this microspot from being saved:</h2>

      <ul>
      <% microspot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="form-group">
    <%= f.label :image %>
     <%= f.file_field :image %>
  </div>


  <div class="form-group">
    <%= f.label :content %>
    <%= f.text_area :content, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :user_id %>
    <%= f.number_field :user_id, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

  </div>
</div>
